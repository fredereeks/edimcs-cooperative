// "use client"

// import {SessionProvider} from "next-auth/react"

// import React from 'react'

// // export default function Provider({provider, children} : {provider: any, children: React.ReactNode | React.ReactNode[]}) {
// //   return (
// //     <>{children}</>
// //   )
// // }

// export const Provider = SessionProvider